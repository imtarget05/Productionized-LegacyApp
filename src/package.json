{
  "name": "legacy-inventory-worker",
  "version": "1.0.0",
  "description": "Legacy worker app for inventory syncing (productionized)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
