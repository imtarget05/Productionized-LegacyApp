apiVersion: v1
kind: Service
metadata:
  name: legacy-app
  labels:
    app: legacy-app
spec:
  selector:
    app: legacy-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
  # ClusterIP by design: external exposure (Ingress) belongs to P03 Phase 7.
  type: ClusterIP
